# openjiuwen-iiap

Python reference implementation of the IIAP DecisionService,
AssistanceService, privacy guard, validators, prompts, and model/agent ports.

`DecisionService` validates every packet against the canonical Schema packaged
in the wheel. See the [Quickstart](../../docs/en/quickstart.md) for source-build
and integration guidance.
