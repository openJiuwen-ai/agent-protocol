add_subdirectory(event)
add_subdirectory(auth)
add_subdirectory(net)
add_subdirectory(shared)
if(MCP_WITH_HTTP AND MCP_BUILD_CLIENT)
    add_subdirectory(http)
elseif(MCP_WITH_HTTP)
    message(STATUS "Skipping http ut tests: requires MCP_BUILD_CLIENT")
endif()
add_subdirectory(log)
if(MCP_BUILD_SERVER)
    add_subdirectory(transport)
else()
    message(STATUS "Skipping transport ut tests: requires MCP_BUILD_SERVER")
endif()

if(MCP_BUILD_CLIENT)
    if(MCP_BUILD_SERVER AND MCP_WITH_HTTP)
        add_subdirectory(client)
    elseif(NOT MCP_BUILD_SERVER)
        message(STATUS "Skipping client ut tests: requires MCP_BUILD_SERVER")
    else()
        message(STATUS "Skipping client ut tests: requires MCP_WITH_HTTP")
    endif()
endif()

if(MCP_BUILD_SERVER)
    if(MCP_WITH_HTTP)
        add_subdirectory(server)
    else()
        message(STATUS "Skipping server ut tests: requires MCP_WITH_HTTP")
    endif()
endif()

if(MCP_ENABLE_STDIO)
    if(MCP_BUILD_SERVER)
        add_subdirectory(stdio)
    else()
        message(STATUS "Skipping stdio ut tests: requires MCP_BUILD_SERVER")
    endif()
endif()

if(MCP_ENABLE_ST_TESTS AND MCP_BUILD_CLIENT AND MCP_BUILD_SERVER AND MCP_WITH_HTTP)
    message(STATUS "Building ST integration tests (tests/ut/st)")
    add_subdirectory(st)
elseif(MCP_ENABLE_ST_TESTS)
    message(STATUS "MCP_ENABLE_ST_TESTS is ON but tests/ut/st requires MCP_BUILD_CLIENT, MCP_BUILD_SERVER, and MCP_WITH_HTTP; skipping st")
endif()
